import { Button, ListGroup } from "react-bootstrap";

const ItemColores = () => {
    return (
             <ListGroup.Item className="d-flex justify-content-between "> Color 1 <Button variant="danger">Eliminar</Button></ListGroup.Item>

    );
};

export default ItemColores;